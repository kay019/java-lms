## 핵심학습목표
DB 테이블이 변경될 때도 스트랭글러 패턴을 적용해 점진적인 리팩터링을 연습한다.
스트랭글러(교살자) 패턴 - 마틴 파울러
스트랭글러 무화과 패턴

## 변경된 기능 요구 사항
- 강의가 진행 중인 상태에서도 수강신청이 가능해야 한다.
  - 강의 진행 상태(준비중, 진행중, 종료)와 모집 상태(비모집중, 모집중)로 상태 값을 분리해야 한다.
- 강의는 하나 이상의 커버 이미지를 가질 수 있다.
- 우아한테크코스(무료), 우아한테크캠프 Pro(유료)와 같이 선발된 인원만 수강 가능해야 한다.
  - 강사는 수강신청한 사람 중 선발된 인원에 대해서만 수강 승인이 가능해야 한다.
  - 강사는 수강신청한 사람 중 선발되지 않은 사람은 수강을 취소할 수 있어야 한다.

## 기능 구현 목록
- [x] 강의 진행 상태(준비중, 진행중, 종료)와 모집 상태(비모집중, 모집중)로 상태 값을 분리한다.
  - [x] 강의 진행 상태 Enum 클래스를 생성한다.
  - [x] 강의 모집 상태 Enum 클래스를 생성한다.
  - [x] 강의 진행 상태와 모집 상태를 강의 도메인에 추가한다.
  - [x] 강의 진행 상태와 모집 상태를 디비 엔티티를 추가한다.
- [x] 강의는 하나 이상의 커버 이미지를 가질 수 있도록 한다.
  - [x] 이미지 개수를 1개 이상 반환하도록 repository 를 수정한다.
- [x] Manager 도메인 클래스를 추가한다.
  - [x] 조건에 따라 수강 승인이 가능해야 한다.
  - [x] 조건에 따라 수강 취소가 가능해야 한다.
